export class BarData {
    max: number;
    usedAll: number;
    used: number;

    constructor(max: number, usedAll: number, used: number) {
        this.max = max;
        this.usedAll = usedAll;
        this.used = used;
    }
}
